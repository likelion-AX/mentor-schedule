# Slack 알림 전수 검증 (notify-schedule-slack)

**검증일**: 2026-07-07 (A~N, 7차에 걸쳐 진행)
**대상 코드**: `js/admin.js`의 `computeScheduleChanges`/`numberSessionsForSlack`/`notifyScheduleSlack`/`notifyMentorSlack`/`notifyDeletedSlack`/`saveProgram`/`assignPerson`/`unassign`/`deleteProgram`/`doProgramMerge`/`checkStaleBeforeSave`/`assignmentsSnapshotKey`, `supabase/functions/notify-schedule-slack/index.ts`의 `formatMessage`/`sessionLines`/`escSlack`

> **아래 본문(A~N)은 검증이 진행된 순서 그대로의 기록이라, 각 라운드 끝의 "종합/최종 요약"은 그 시점까지의
> 스냅샷일 뿐 최신 상태가 아니다. 지금 기준으로 뭐가 고쳐졌고 뭐가 남았는지는 맨 아래
> "전체 최종 결론"만 보면 된다.**

## 방법

실제 소스 코드에서 알림 생성 로직을 그대로 옮겨온 시뮬레이션 하네스(`scratchpad/notify-sim.js`, `notify-scenarios.js`)로, DB나 Slack을 전혀 건드리지 않고 각 상황이 실제로 어떤 텍스트를 만들어내는지 순수 함수로 재현했다. Slack에 실제로 전송된 것은 하나도 없다.

- 포함: `computeScheduleChanges`(회차 diff), `numberSessionsForSlack`(회차 번호/정렬), `formatMessage`(최종 문구), 각 알림 함수의 가드 조건(무변경 스킵, 배정 실패 시 폴백 등), 엣지함수의 요청 검증(`validateBody`)까지 전 구간.
- 제외(이번 검증 범위 아님): `nameOf()`의 실제 이메일→이름 조회, RLS/네트워크 계층, Slack 실제 전송·렌더링 결과 자체. 아래서 발견한 마크다운 이슈는 "Slack이 이렇게 렌더링할 것"이라는 문법 지식 기반 추론이지 실제 Slack 화면 캡처는 아니다.

총 30개 시나리오(A~G) 실행. 결과 전문은 아래 각 섹션에 그대로 첨부하고, 항목별로 확인한 내용을 적었다.

---

## A. 새 교육 등록 (created)

### A-1. 회차 1개, 멘토 미지정
```
📅 새 교육 일정이 확정됐습니다 — *[테스트 회사]* 테스트 제목
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토: 미정
🔗 앱에서 확인: https://likelion-ax.github.io/mentor-schedule/admin.html?program=...
```
**확인**: 정상. "미정" 폴백 정상 동작.

### A-2. 회차 3개 + 멘토 2명 함께 등록
```
📅 새 교육 일정이 확정됐습니다 — *[테스트 회사]* 테스트 제목
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
• 2회차 8/11(화) 10:00~12:00 · 테스트 장소
• 3회차 8/18(화) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원, 이지현
🔗 앱에서 확인: ...
```
**확인**: 정상. 멘토를 등록과 동시에 넣어도 이 메시지 1건만 나가고 별도 "배정" 알림이 중복으로 안 나감(설계대로).

### A-3. 퍼실만 지정, 멘토 없음
```
📅 새 교육 일정이 확정됐습니다 — *[테스트 회사]* 테스트 제목
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토: 미정
🔗 앱에서 확인: ...
```
**확인**: 정상. 퍼실만 있어도 "담당 멘토"엔 안 나오고 미정으로 표시(의도한 대로 — 퍼실은 알림 대상 아님).

### A-4. 하루 2회차, 입력 순서가 시간 역순이어도
```
📅 새 교육 일정이 확정됐습니다 — *[테스트 회사]* 테스트 제목
• 1회차 8/4(화) 09:00~11:00 · A홀
• 2회차 8/4(화) 14:00~16:00 · B홀
• 3회차 8/11(화) 10:00~12:00 · A홀
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 폼에 오후 타임을 먼저 입력해도 회차 번호는 시간순으로 정렬됨(정렬 키에 시작시간 tie-break 추가한 효과).

---

## B. 일정 편집 (updated)

### B-1. 무변경 재저장
```
(알림 없음 — 코드 가드에서 조용히 스킵됨)
```
**확인**: 정상. `notifyScheduleSlack`의 `changes.length === 0` 가드가 정확히 걸러냄.

### B-2. 같은 회차, 시간만 변경
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 · 테스트 장소 → 8/4(화) 11:00~13:00 · 테스트 장소

전체 일정:
• 1회차 8/4(화) 11:00~13:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 날짜가 같아도 양쪽 다 표시해서 다소 반복적이지만(스타일 문제일 뿐, 오류 아님), 정보는 정확함.

### B-3. 같은 회차, 장소만 변경
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 · 테스트 장소 → 8/4(화) 10:00~12:00 · 새 장소

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 새 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상.

### B-4. 같은 회차, 날짜 필드만 직접 수정(행 삭제 안 함)
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 · 테스트 장소 → 8/20(목) 10:00~12:00 · 테스트 장소

전체 일정:
• 1회차 8/20(목) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상 — 이번에 id 매칭으로 바꾸면서 같이 고쳐진 부분. 예전 로직(날짜 기준 매칭)이었다면 이 케이스가 "삭제 8/4 + 추가 8/20"으로 잘못 나왔을 것. 지금은 하나의 "바뀜"으로 정확히 나옴.

### B-5. 회차 1개 신규 추가만
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• (추가) 8/11(화) 10:00~12:00 · 테스트 장소

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
• 2회차 8/11(화) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상.

### B-6. 회차 1개 삭제만
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• (삭제) 8/11(화) 10:00~12:00 · 테스트 장소

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상.

### B-7. 무관한 삭제+추가 동시(다른 날짜, 실제 "테스트2" 시나리오)
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• (삭제) 8/17(월) 13:00~15:00 · 삼성역
• (추가) 8/31(월) 13:00~15:00 · 삼성역

전체 일정:
• 1회차 8/4(화) 13:00~15:00 · 삼성역
• 2회차 8/25(화) 13:00~15:00 · 삼성역
• 3회차 8/31(월) 13:00~15:00 · 삼성역
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 이전에 실사용자 시나리오로 이미 검증했던 것과 동일 결과 재확인(회귀 없음).

### B-8. [핵심 회귀 테스트] 하루 2회차 중 하나 삭제+무관한 신규 추가, 다른 하나는 시간만 변경
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• (삭제) 8/4(화) 10:00~12:00 · A홀
• 8/4(화) 14:00~16:00 · B홀 → 8/4(화) 15:00~17:00 · B홀
• (추가) 8/4(화) 09:00~11:00 · C홀

전체 일정:
• 1회차 8/4(화) 09:00~11:00 · C홀
• 2회차 8/4(화) 15:00~17:00 · B홀
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: **바로 이전에 발견했던 버그의 재발 여부 확인용.** 실제로 일어난 일(A홀 삭제, B홀은 시간만 변경, C홀 무관 신규 추가)과 메시지가 정확히 일치. id 기반 매칭으로 바꾼 뒤 이 케이스가 완전히 고쳐졌음을 재확인.

### B-9. 무변경 1 + 시간변경 1 + 삭제 1 + 추가 1, 한 번에
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/11(화) 10:00~12:00 · 장소1 → 8/11(화) 13:00~15:00 · 장소1
• (삭제) 8/18(화) 10:00~12:00 · 장소1
• (추가) 8/25(화) 10:00~12:00 · 장소1

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 장소1
• 2회차 8/11(화) 13:00~15:00 · 장소1
• 3회차 8/25(화) 10:00~12:00 · 장소1
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 무변경 회차(8/4)는 변경사항 목록에 안 나오면서 전체 일정에는 정확히 포함됨.

### B-10. 장소가 원래 비어있는 회차의 시간만 변경
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 → 8/4(화) 11:00~13:00

전체 일정:
• 1회차 8/4(화) 11:00~13:00
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 장소가 없으면 "· " 자체가 안 붙어서 깔끔함(dangling separator 없음).

### B-11. 하루 3+1회차 동시 변경(무변경1+시간변경1+삭제1+추가1, 전부 같은 날)
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 11:00~12:00 · 2홀 → 8/4(화) 11:30~12:30 · 2홀
• (삭제) 8/4(화) 13:00~14:00 · 3홀
• (추가) 8/4(화) 15:00~16:00 · 4홀

전체 일정:
• 1회차 8/4(화) 09:00~10:00 · 1홀
• 2회차 8/4(화) 11:30~12:30 · 2홀
• 3회차 8/4(화) 15:00~16:00 · 4홀
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 하루에 회차가 3~4개로 늘어도(2개일 때만 테스트했던 B-8보다 더 극단적) id 기반 매칭이라 전부 정확하게 분리됨.

---

## C. 멘토 배정/변경(스케줄 불변)

### C-1. 멘토 0명 → 1명(assigned)
```
👤 멘토가 배정됐습니다 — *[테스트 회사]* 테스트 제목

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상.

### C-2. 멘토 1명 있는 상태에서 1명 추가
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 이지현 추가

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 김사원, 이지현
🔗 앱에서 확인: ...
```
**확인**: 정상.

### C-3. 마지막 멘토 1명 해제
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
- 김사원 제외

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 미정
🔗 앱에서 확인: ...
```
**확인**: 정상. 다 빠져도 "미정" 폴백이 "담당 멘토(현재)" 라벨에서도 정상 동작.

### C-4. 멘토 교체(제외+추가 동시)
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 운영팀 추가
- 김사원 제외

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 운영팀
🔗 앱에서 확인: ...
```
**확인**: 정상.

### C-5. 퍼실만 추가, 멘토 구성은 그대로
```
(알림 없음 — 코드 가드에서 조용히 스킵됨)
```
**확인**: 정상. 퍼실 변경은 멘토 diff에 영향이 없어 애초에 added/removed가 둘 다 비어서 알림이 안 나감.

### C-6. 멘토 1명 추가 + 퍼실 1명 추가 동시
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 이지현 추가

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 김사원, 이지현
🔗 앱에서 확인: ...
```
**확인**: 정상. 퍼실 추가분은 전혀 안 보이고 멘토 변경분만 나옴.

---

## D. 스케줄+멘토 동시 변경

### D-1. 일정 시간 변경 + 멘토 교체를 같은 저장에서 동시에
```
--- 메시지 1/2 ---
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 · 테스트 장소 → 8/4(화) 11:00~13:00 · 테스트 장소

전체 일정:
• 1회차 8/4(화) 11:00~13:00 · 테스트 장소
담당 멘토: 운영팀
🔗 앱에서 확인: ...
--- 메시지 2/2 ---
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 운영팀 추가
- 김사원 제외

전체 일정:
• 1회차 8/4(화) 11:00~13:00 · 테스트 장소
담당 멘토(현재): 운영팀
🔗 앱에서 확인: ...
```
**확인**: 정상. 2건 다 나가고, 각각 독립적으로 완전한 정보(전체 일정 포함)를 담고 있어 순서와 무관하게 이해 가능.

---

## E. 모달 밖 개별 액션

### E-1. 목록에서 바로 멘토 배정(기존 멘토 1명 있는 상태)
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 신규멘토 추가

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 김사원, 신규멘토
🔗 앱에서 확인: ...
```
**확인**: 정상.

### E-2. 목록에서 바로 퍼실 배정
```
(알림 없음 — 코드 가드에서 조용히 스킵됨)
```
**확인**: 코드상 `assignPerson()`의 `if (type === "mentor") notifyMentorSlack(...)` 가드로 애초에 함수 호출 자체가 안 됨(코드 읽기로 확인, 이번 시뮬레이션은 이 가드를 실행하진 않고 결과만 반영).

### E-3. 확정 멘토 배정 해제
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
- 김사원 제외

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 이지현
🔗 앱에서 확인: ...
```
**확인**: 정상.

### E-4. 확정 퍼실 배정 해제
```
(알림 없음 — 코드 가드에서 조용히 스킵됨)
```
**확인**: `unassign()`의 `a.staff_type === "mentor"` 가드로 애초에 호출 안 됨(E-2와 동일하게 코드 읽기로 확인).

---

## F. 배정 저장 실패(assignErr) — 성사 안 된 변경을 알리지 않는지

### F-1. 신규 생성 중 멘토 배정 insert 실패
```
📅 새 교육 일정이 확정됐습니다 — *[테스트 회사]* 테스트 제목
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토: 미정
🔗 앱에서 확인: ...
```
**확인**: 정상. 멘토를 지정해서 만들려 했지만 실패했으므로, 사실과 다르게 "배정됨"이라 말하지 않고 "미정"으로 정확히 표시.

### F-2. 일정 변경 + 멘토 배정 insert 동시 실패
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 · 테스트 장소 → 8/4(화) 11:00~13:00 · 테스트 장소

전체 일정:
• 1회차 8/4(화) 11:00~13:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
(멘토 변경 알림은 발생하지 않음 — assignErr 가드로 함수 호출 자체가 생략됨)
```
**확인**: 정상. 일정 변경은 실제로 성공했으니 ✏️는 정상 발송되고, 멘토명은 실패 이전 상태(김사원)를 정직하게 보여줌. 실패한 멘토 변경(🔄)은 안 나감.

---

## G. 그 외 — 발견된 문제

### G-1. ⚠️ **확인된 문제**: 회사명에 Slack 마크다운 특수문자(`*`, `_`, `` ` ``)가 있으면 렌더링이 깨짐
입력: `client: "테스트*회사_이름"`
```
📅 새 교육 일정이 확정됐습니다 — *[테스트*회사_이름]* AX교육
• 1회차 8/4(화) 10:00~12:00 · 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
텍스트 자체는 이렇게 나오지만, 이걸 실제 Slack이 mrkdwn으로 렌더링하면 `*[테스트` 에서 첫 `*`가 볼드를 열고 이름 중간의 `*`가 그걸 조기에 닫아버려서, 의도한 것과 다르게 "**[테스트**회사_이름]* AX교육" 식으로 볼드 범위가 뒤틀리고 `_이름` 부분은 기울임으로 잘못 해석될 수 있음. 회사명·교육명·멘토 이름 등 사용자가 자유롭게 입력하는 텍스트를 그대로 `*...*`로 감싸는 모든 지점(`formatMessage`의 헤더 라인)이 해당됨.
**심각도**: 낮음~중간(실제 회사명에 `*`/`_`/백틱이 들어갈 일은 드물지만, 있으면 확실히 깨짐). 원한다면 별도로 고칠 수 있음(예: 헤더의 굵게 처리를 없애거나, 특수문자를 이스케이프).

### G-2. 회사명·제목이 둘 다 빈 문자열인 경우 헤더가 지저분하게 나옴(`— *[]*` + 끝에 공백)
```
📅 새 교육 일정이 확정됐습니다 — *[]* 
• 1회차 8/4(화) 10:00~12:00 · 장소
담당 멘토: 미정
🔗 앱에서 확인: ...
```
**확인**: `saveProgram()`에 `if (!base.client && !base.title) return Util.toast(...)` 프론트 검증이 있어 **앱 UI로는 이 상황 자체에 도달 불가능**(회사/제목 둘 다 비우면 저장이 아예 안 됨). 엣지함수 자체에는 이 검증이 없어서, 앱을 거치지 않고 API를 직접 호출하면 이론상 가능. 실전 영향은 없음 — 참고용으로만 기록.

---

## H. 2차 검증 — "그 외 정말 없나?" 다시 파고든 결과

### H-1. 멘토 이름 자체에 마크다운 특수문자가 있으면 G-1과 같은 문제
입력: `beforeNames=["김사원"]`, `afterNames=["김사원","박*민수"]`
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 박*민수 추가

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 김사원, 박*민수
🔗 앱에서 확인: ...
```
**확인**: G-1과 같은 원인. "박*민수"의 `*`는 그 뒤로 짝을 지을 `*`가 메시지에 하나도 안 남아있어서(전체 일정·담당 멘토 줄엔 `*`가 없음), 볼드가 메시지 끝까지 안 닫힌 채로 이어질 수 있음. G-1과 같이 묶어서 고치면 됨(회사명뿐 아니라 이름에도 같은 처리 필요).

### H-2. ⚠️ **확인된 버그(가장 심각)**: 동명이인으로 멘토가 바뀌면 알림이 통째로 사라짐
입력: 실제로는 서로 다른 사람(이메일이 다름)인데 표시 이름이 둘 다 "김민수"인 경우 — 한 "김민수"를 빼고 다른 "김민수"를 넣음(`beforeNames=["김민수"]`, `afterNames=["김민수"]`)
```
(알림 없음 — 코드 가드에서 조용히 스킵됨)
```
**원인**: `notifyMentorSlack`의 `added`/`removed` 계산이 이메일이 아니라 **표시 이름 문자열**로 비교함(`afterNames.filter(n => !beforeNames.includes(n))`). 실제로는 담당자가 바뀌었는데 이름이 같으면 배열 내용이 똑같아 보여서 "변경 없음"으로 처리돼버림.
**심각도**: 현재 로스터(운영팀·이지현·정한결·김승한·이유진·문경필·최무희·홍승철·김사원·이강권)엔 동명이인이 없어서 지금 당장 터지는 문제는 아니지만, 나중에 이름이 같은 멘토가 들어오면 그 사람으로 담당자가 바뀌어도 **아무도 모르게 조용히 넘어감** — 정확히 알림 시스템이 막으려던 그 상황(담당자 변경을 놓치는 것)이 이름 우연히 겹치는 것만으로 재발할 수 있음. 이건 회사명 마크다운 이슈보다 실질적으로 더 위험하다고 봐.

### H-3. 이름이 같은 두 사람을 동시에 추가하면 문구가 앞뒤로 안 맞음
입력: 기존 멘토 1명 있는 상태에서, 이메일은 다르지만 이름이 같은 두 사람("김민수")을 동시에 추가
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 김민수 추가
+ 김민수 추가

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 기존멘토, 김민수
🔗 앱에서 확인: ...
```
**확인**: "+ 김민수 추가"가 두 번 나오는데(실제로 다른 두 사람이 추가된 거라 맞는 표시), 맨 아래 "담당 멘토(현재)"는 `Set`으로 중복 제거돼서 "김민수"가 한 번만 나옴 — 두 명 추가됐다고 위에서 말해놓고 아래선 한 명만 있는 것처럼 보여서 앞뒤가 안 맞음. H-2와 같은 근본 원인(이름 기준 처리)에서 파생됨.

### H-4. 회차가 아주 많은 프로그램(24회차, 6개월 매주) — 버그는 아니고 참고사항
```
(24줄짜리 회차 목록, 총 1022자/27줄 — 본문 생략, 형식은 다른 케이스와 동일)
```
**확인**: 버그 아님. Slack 메시지 길이 제한(약 4만자)엔 전혀 안 걸리지만, 27줄이면 Slack이 자동으로 "더 보기" 접힘 UI를 씌울 수 있음 — 알아두면 좋은 사실 정도.

---

## 종합

| 항목 | 결과 |
|---|---|
| 스케줄 생성/수정 diff 전 케이스(A, B, 11개) | 전부 정상, 특히 핵심 회귀 케이스(B-8, B-11) 통과 |
| 멘토 배정/변경 케이스(C, D, E, 12개) | 전부 정상, 퍼실 제외 로직 정상 |
| 배정 실패 시 정직성(F, 2개) | 전부 정상 |
| 엣지함수 자체 요청 검증(모든 케이스) | 프론트가 만든 body가 전부 통과 — 프론트/백엔드 불일치 없음 |
| 특수 케이스 1차(G, 2개) | **G-1 확인된 버그**(마크다운 특수문자), G-2는 실전 도달 불가능한 이론상 케이스 |
| 2차 검증(H, 4개) | **H-2 확인된 버그(심각, false negative)**, **H-3 확인된 버그(경미, 표시 불일치)**, H-1은 G-1과 동일 부류, H-4는 버그 아님 |

**정리하면 실제로 고칠 가치가 있는 건 3가지**:
1. **G-1 + H-1**: 회사명·교육명·멘토 이름에 마크다운 특수문자(`*`,`_`,`` ` ``)가 있으면 Slack 렌더링이 깨짐.
2. **H-2 (가장 중요)**: 멘토 배정/변경 diff가 이메일이 아니라 표시 이름으로 비교돼서, 동명이인으로 담당자가 바뀌면 알림이 아예 안 나감.
3. **H-3**: H-2와 같은 원인 — 이름이 같은 두 사람이 동시에 추가/제외되면 문구가 서로 모순돼 보임.

2번과 3번은 diff 로직을 이메일 기준으로 바꾸면 한 번에 같이 고쳐질 것 같아. 고칠지 알려줘.

---

## 수정 완료 (2026-07-07)

**1. 마크다운 특수문자 이스케이프 (G-1, H-1)**
`notify-schedule-slack/index.ts`에 `escSlack()` 추가 — `&`/`<`/`>`는 Slack 공식 엔티티로, `*`/`_`/`~`/`` ` ``는 각 문자 바로 뒤에 폭 없는 공백(U+200B)을 붙여 서식 페어링을 끊음. 회사명·교육명·멘토 이름·회차 장소 등 사용자가 입력하는 모든 자유 텍스트 삽입 지점에 적용.

재검증:
```
📅 새 교육 일정이 확정됐습니다 — *[테스트*​회사_​이름]* AX교육   ← * 와 _ 뒤에 폭 없는 공백이 붙어 더 이상 서식 문자로 안 읽힘
```

**2. 멘토 diff를 이메일 기준으로 변경 (H-2, H-3)**
`confirmedAssigneeNames`→`confirmedAssigneeEmails`로, `notifyMentorSlack`의 `beforeNames`/`afterNames`→`beforeEmails`/`afterEmails`로 전부 변경(`saveProgram`/`assignPerson`/`unassign` 호출부 포함). 표시 이름 변환은 `notifyMentorSlack` 안에서 body를 만드는 마지막 순간에만 한다.

재검증(H-2, 가장 중요한 케이스 — 실제로는 다른 사람인데 표시 이름이 둘 다 "김민수"인 경우):
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 김민수 추가
- 김민수 제외

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
담당 멘토(현재): 김민수
🔗 앱에서 확인: ...
```
전에는 이 케이스가 알림이 아예 안 나갔는데, 이제 "+ 추가"/"- 제외"가 정확히 나감 — 실제로 서로 다른 사람이 바뀐 거라는 게 이메일 기준 diff로 잡힘.

**남은 잔여 이슈(참고용, 안 고침)**: H-3(이름이 같은 두 사람을 동시에 추가하면 "+ 김민수 추가"가 두 번 나오는데 맨 아래 "담당 멘토(현재)"는 이름 기준 `Set`이라 한 번만 표시됨)은 diff 자체는 이제 정확하지만, 화면 표시는 여전히 이름으로만 중복 제거함. 이걸 이메일 기준으로도 완전히 고치려면 "김민수" 두 명을 화면에 어떻게 구분해서 보여줄지(이메일 병기 등) 추가 설계가 필요해서, 지금은 손대지 않음 — 두 명의 서로 다른 멘토가 완전히 같은 표시 이름을 갖고 동시에 배정되는 경우에만 발생하는 드문 조합이라 우선순위 낮음.

전 시나리오(A~H, 30개+) 재실행해서 회귀 없음 확인 완료.

---

## I. 3차 검증 — 지금까지와 다른 각도로 다시 발산

앞선 두 라운드는 전부 "일정/멘토가 실제로 바뀐 경우"의 diff·문구 정확도에 집중했다. 이번엔 방향을 바꿔서 (1) 알림 시스템이 아예 신경 쓰지 않는 필드가 있는지, (2) 순서/구조가 바뀌지만 내용은 그대로인 경우, (3) 입력값의 극단적인 형태(길이·날짜) 세 갈래로 다시 훑었다.

### I-1. ⚠️ **확인된 문제(새로 발견, 이번 라운드에서 가장 중요)**: 세션은 그대로인데 회사명·상태·필요인원 같은 프로그램 메타데이터만 바뀌면 알림이 전혀 안 나감
입력: 세션(날짜/시간/장소) 완전히 동일, `client`만 "새회사명(변경됨)"으로 변경
```
computeScheduleChanges 결과: []
(알림 없음 -- 회사명이 바뀌었는데도 아무 알림도 안 감)
```
**원인**: `computeScheduleChanges`의 `norm()`이 `id/date/start_time/end_time/location`만 비교한다. `education_sessions`의 `client`/`title`/`status`/`needed_mentors`/`needed_facilitators`/`memo` 컬럼은 매 저장마다 세션 행에 함께 갱신되지만, diff 비교 대상에 아예 안 들어가 있어서 이 필드들만 바뀌면 `changes`가 항상 빈 배열이 되고 `notifyScheduleSlack`의 "무변경" 가드에 걸려 알림 자체가 안 나간다.

### I-2. 위와 같은 원인 — 프로그램 상태를 "취소"로 바꿔도 알림 없음
```
changes: []
(알림 없음)
```
**확인**: 회차/멘토가 전혀 안 바뀐 채로 상태만 "모집중 → 취소"로 바뀌는 건 운영상 꽤 중요한 이벤트인데(교육이 아예 없어진다는 뜻), 지금 알림 체계엔 이 케이스를 다루는 경로가 전혀 없다. `admin.js` 전체를 찾아봐도 "취소"와 관련된 Slack 알림 코드는 없음(취소는 그냥 상태값 저장일 뿐).

### I-3. 같은 원인 — 필요 멘토 인원(needed_mentors)만 1→3으로 바꿔도 알림 없음
```
changes: []
(알림 없음)
```
**확인**: "이제 멘토가 2명 더 필요해요" 같은 정보도 지금은 회차 자체를 안 건드리면 전달할 방법이 없음.

### I-4. 회차 "순서"만 재배열되고 내용은 완전히 동일 — 무변경으로 정확히 처리되는지 (회귀 확인)
입력: 폼에서 회차 행 순서만 뒤집음(내용은 3개 다 그대로)
```
changes: []
(알림 없음 — 정상)
```
**확인**: 정상. id 기반 매칭이라 폼에 나열된 순서가 뭐든 상관없이 정확히 무변경으로 처리됨(순서 자체는 diff 대상이 아님 — 애초에 맞는 설계).

### I-5. 기본 장소(전체 회차 공통 장소)만 바꾸면, 오버라이드 없는 회차들에 자동 반영되어 정확히 감지되는지
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 · 기본장소 → 8/4(화) 10:00~12:00 · 새기본장소
• 8/11(화) 10:00~12:00 · 기본장소 → 8/11(화) 10:00~12:00 · 새기본장소

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 새기본장소
• 2회차 8/11(화) 10:00~12:00 · 새기본장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 기본 장소는 회차별 `location`에 그대로 반영되니까(개별 오버라이드가 없는 한) `computeScheduleChanges`가 정확히 잡아냄 — I-1/I-2/I-3과 달리 이건 실제로 `location` 필드를 통해서 감지되는 경로라 문제 없음.

### I-6. 아주 긴 주소(실제 데이터에 있던 형태 참고)
```
📅 새 교육 일정이 확정됐습니다 — *[테스트 회사]* 테스트 제목
• 1회차 8/4(화) 10:00~12:00 · 대한민국 서울특별시 강남구 테헤란로 521 아셈타워 27층 라이언즈 AX 캠퍼스 3세미나실(주차는 지하 3층 이용)
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. 잘리거나 깨지지 않음(Slack이 화면에서 알아서 줄바꿈).

### I-7. 아주 먼 미래 날짜(2099-12-31)
```
📅 새 교육 일정이 확정됐습니다 — *[테스트 회사]* 테스트 제목
• 1회차 12/31(목) 10:00~12:00 · 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```
**확인**: 정상. `fmtDate`가 연도와 무관하게 월/일/요일만 뽑는 구조라 문제없음.

---

## 종합(갱신)

| 항목 | 결과 |
|---|---|
| 1차(A~G) | G-1 발견 → 수정 완료 |
| 2차(H) | H-2(심각) 발견 → 수정 완료, H-1은 G-1과 함께 수정, H-3은 잔여(우선순위 낮음, 안 고침) |
| 3차(I) | **I-1/I-2/I-3 발견(신규, 미수정)** — 세션이 안 바뀌면 회사명·상태·필요인원 변경은 알림이 전혀 안 나감. I-4/I-5/I-6/I-7은 전부 정상(회귀 없음) |

**I-1~I-3을 고칠지는 설계 판단이 필요해서 일단 보고만 함**: 이건 지금까지처럼 "이미 있는 diff 로직의 버그"가 아니라 "애초에 이 필드들을 감시 대상에 넣을지 말지"의 스코프 문제에 가까움. 특히 I-2(상태→취소)는 운영상 알림 가치가 있어 보이는데, 고치려면 (a) `computeScheduleChanges`가 세션 레벨이 아니라 프로그램 레벨 필드도 비교하게 확장하거나, (b) 상태 변경만 별도의 5번째 알림 타입("🚫 교육이 취소됐습니다" 같은)으로 신설하는 두 방향이 있음.

**결정**: (b) 별도 알림 타입으로 진행. 이유: ① 상태 변경(특히 취소)은 "일정이 수정됐다"와 성격이 다른 이벤트라 ✏️(사소한 시간·장소 조정에도 매번 뜨는 알림)에 묻히면 놓치기 쉬움 — 지금까지 4개 타입을 전부 의미 단위로 쪼갠 것과 같은 논리. ② 검증 끝난 `computeScheduleChanges`(세션 diff)를 안 건드리는 완전히 독립된 추가라 회귀 위험이 없음. `needed_mentors`/`needed_facilitators`/`client`/`title`/`memo` 변경은 이번 스코프에서 제외(내부 계획 조정에 가까워 팀 전체에 실시간으로 알릴 만큼 급한 정보는 아니라고 판단) — **오직 `status`가 "취소"로 들어가거나 "취소"에서 나오는 경우만** 알림.

---

## J. 설계 제안 — 5번째 알림 타입(상태 변경: 취소/복구)

### 트리거 조건
`saveProgram()`에서 `state.programById[id]?.status`(저장 전 상태)와 `base.status`(폼 값)를 비교:
- `이전 !== "취소" && 이후 === "취소"` → **cancelled**
- `이전 === "취소" && 이후 !== "취소"` → **restored**(취소했다가 되돌린 경우 — 실수로 취소했거나 다시 진행하기로 한 경우 대비)
- 그 외 상태 전환(모집중→확정→완료 등)은 지금처럼 알림 없음(스코프 밖).
- 새 프로그램 생성(`id` 없음)은 "이전 상태"가 없으므로 해당 없음.
- 같은 저장에서 회차/멘토도 같이 바뀌면 ✏️/🔄와 **함께** 독립적으로 나감(D-1에서 이미 검증한 "동시 변경 시 각자 완전한 메시지로 따로 나가는" 원칙 그대로 적용).

### 문구 예시(시뮬레이션으로 직접 뽑아본 것)

**J-1. 취소로 전환**
```
🚫 교육이 취소됐습니다 — *[테스트 회사]* 테스트 제목

전체 일정(취소 전 마지막 상태):
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
• 2회차 8/11(화) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```

**J-2. 취소에서 복구**
```
↩️ 취소됐던 교육이 다시 살아났습니다 — *[테스트 회사]* 테스트 제목

전체 일정:
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
• 2회차 8/11(화) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원
🔗 앱에서 확인: ...
```

**J-3. 멘토 미배정 상태로 취소**
```
🚫 교육이 취소됐습니다 — *[테스트 회사]* 테스트 제목

전체 일정(취소 전 마지막 상태):
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
• 2회차 8/11(화) 10:00~12:00 · 테스트 장소
담당 멘토: 미정
🔗 앱에서 확인: ...
```

### 구현 계획
- `notify-schedule-slack/index.ts`: `Action`에 `"cancelled" | "restored"` 추가, `HEADERS`에 두 문구 추가, `formatMessage`에 두 액션 분기 추가(전체 일정 + 담당 멘토만 보여주는 단순한 형태 — diff 목록 없음). 유효성 검증에도 `sessions` 필수 조건 추가(다른 타입과 동일 원칙).
- `admin.js`: `saveProgram()`에 `const prevStatus = state.programById[id]?.status;` 추가하고, insert/delete 이후(다른 알림들과 같은 타이밍) `prevStatus`와 `base.status`를 비교해서 `notifyStatusSlack()`(신설) 호출. 회차/멘토 알림과 완전히 독립적으로 fire.
- 새 함수 `notifyStatusSlack(pid, base, prevStatus, sessions, mentorNames)`: `prevStatus === base.status`면 조용히 리턴(무변경 가드, 기존 패턴과 동일).

이 방향과 문구로 진행할지, 바꾸고 싶은 부분(이모지, 문구, "복구" 알림 자체가 필요한지 등) 있으면 알려줘 — 확정되면 바로 구현할게.

**➜ 방향 전환**: 실제 DB를 확인해보니 `status="취소"`인 행이 현재 0개 — 실제로는 취소하면 상태만 바꾸는 게 아니라 `deleteProgram()`으로 아예 지워버리는 게 실사용 패턴이라, 위 설계(J)는 일어나지 않는 상황을 대비하는 셈이었음. 그래서 "상태 변경 알림" 대신 **삭제 자체에 대한 알림**으로 방향을 바꿈 — 아래 K 섹션 참고.

---

## K. 구현 완료 — 5번째 알림 타입(교육 삭제)

### 트리거
`deleteProgram()`에서 실제 DB 삭제(`assignments`/`education_sessions` delete)가 성공하면 무조건 호출. 삭제되고 나면 `state.programById[id]`가 사라지므로, 회차·멘토 스냅샷은 **삭제 DB 콜을 날리기 전에** 미리 떠 둔다.

### 문구 예시(재검증 완료)

**K-1. 멘토 배정된 교육 삭제**
```
🗑️ 교육이 삭제됐습니다 — *[테스트 회사]* 테스트 제목
전체 일정(삭제 전 마지막 상태):
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
• 2회차 8/11(화) 10:00~12:00 · 테스트 장소
담당 멘토: 김사원
```
(🔗 앱에서 확인 링크는 의도적으로 없음 — 삭제된 program_id는 더 이상 존재하지 않아 눌러도 아무것도 안 뜸.)

**K-2. 멘토 미배정 상태로 삭제**
```
🗑️ 교육이 삭제됐습니다 — *[테스트 회사]* 테스트 제목
전체 일정(삭제 전 마지막 상태):
• 1회차 8/4(화) 10:00~12:00 · 테스트 장소
• 2회차 8/11(화) 10:00~12:00 · 테스트 장소
담당 멘토: 미정
```

### 구현
- `notify-schedule-slack/index.ts`: `Action`에 `"deleted"` 추가, `HEADERS`/`formatMessage`/요청 검증에 반영. `deleted`일 때만 앱 링크 줄 자체를 생략.
- `admin.js`: `notifyDeletedSlack(pid, base, sessions, mentorNames)` 신설. `deleteProgram()`에서 DB 삭제 호출 전에 `p = state.programById[id]`, `mentorNames`, `sessionsSnapshot`을 미리 캡처해두고, 삭제 성공 후 호출.
- 유효성 검증: `deleted`도 `created`/`assigned`/`changed`와 동일하게 `sessions` 필수(빈 배열로 오면 400) — 실제로 프로그램은 항상 회차 1개 이상이라 도달 불가능한 방어선이지만 다른 타입들과 원칙 통일.

전체 시나리오(27개) 재실행 + 신규 K-1/K-2 검증까지 회귀 없음 확인. 배포 완료.

---

## L. 4차 검증 — 삭제 알림(deleted) 집중 스트레스 테스트

새로 추가된 지 얼마 안 된 기능이라, 다른 4개 타입에 했던 것과 같은 강도로 따로 훑었다(마크다운 이스케이프, 같은 날 여러 회차, 대량 회차, 방어선, 다중 멘토).

### L-1. 회사명·멘토 이름에 마크다운 특수문자 포함된 프로그램 삭제
```
🗑️ 교육이 삭제됐습니다 — *[테스트*​회사_​이름]* AX교육
전체 일정(삭제 전 마지막 상태):
• 1회차 8/4(화) 10:00~12:00 · 장소
담당 멘토: 박*​민수
```
**확인**: 정상. `escSlack`이 다른 타입들과 동일하게 적용됨(`formatMessage` 공용 함수라 당연하지만 실제로 확인).

### L-2. 하루 2회차 있는 프로그램 삭제(회차 번호/정렬)
```
🗑️ 교육이 삭제됐습니다 — *[테스트 회사]* 테스트 제목
전체 일정(삭제 전 마지막 상태):
• 1회차 8/4(화) 09:00~11:00 · A홀
• 2회차 8/4(화) 14:00~16:00 · B홀
담당 멘토: 김사원
```
**확인**: 정상. 시간순 정렬·회차 번호 정상.

### L-3. 회차 24개짜리 대형 프로그램 삭제
```
(24줄 전체 일정 목록 정상 출력 — 본문 생략)
```
**확인**: 정상. 스케일 문제 없음.

### L-4. sessions 없이 삭제 알림 시도(방어선)
```
!!! 거부됨: sessions가 필요합니다.
```
**확인**: 정상. 실제로는 프로그램이 항상 회차 1개 이상이라 도달 불가능하지만, 방어선 자체가 살아있음을 재확인.

### L-5. 멘토 2명 배정된 채로 삭제
```
🗑️ 교육이 삭제됐습니다 — *[테스트 회사]* 테스트 제목
전체 일정(삭제 전 마지막 상태):
• 1회차 8/4(화) 10:00~12:00 · 장소
담당 멘토: 김사원, 이지현
```
**확인**: 정상.

전체 스크립트(A~L, 32개 시나리오) 재실행 결과 이상 없음(`거부됨`/`undefined`/`NaN` 등 이상 문자열 없음, grep으로 확인).

---

## 최종 요약 — 5개 알림 타입 전체 커버리지

| 알림 타입 | 트리거 | 검증 라운드 | 상태 |
|---|---|---|---|
| 📅 created(새 일정) | 새 프로그램 저장 | A | 정상 |
| ✏️ updated(일정 수정) | 기존 회차 diff 발생 | B, I(부분), L | 정상 — 단, 세션 안 바뀌고 메타데이터(회사명/상태/필요인원)만 바뀌면 감지 안 됨(I-1~I-3, 의도적 스코프 제외) |
| 👤 assigned / 🔄 changed(멘토) | 확정 멘토 구성 diff(이메일 기준) | C, D, E, F, H | 정상 — H-2(동명이인 false negative) 수정 완료 |
| 🗑️ deleted(교육 삭제) | `deleteProgram()` 성공 | K, L | 정상 |

**전체 발견 이력**: G-1(마크다운 특수문자) → 수정 완료. H-2(동명이인 false negative, 가장 심각) → 수정 완료. H-1은 G-1과 함께 수정. H-3(동명이인 동시 추가 시 표시 중복)은 잔여, 우선순위 낮아 보류. I-1~I-3(메타데이터 단독 변경 무알림)은 스코프 판단 후 "취소" 케이스만 별도 대응하기로 했다가, 실사용 데이터 확인 후 "삭제 알림"으로 방향 전환해 K로 구현 완료. needed_mentors 등 나머지 메타데이터는 여전히 무알림(의도적 제외).

**남은 것(참고, 우선순위 낮음)**: H-3 하나뿐 — 이름이 완전히 같은 두 멘토가 동시에 배정될 때만 나타나는 표시 중복.

---

## M. 5차 검증 — 메시지 문구가 아니라 "구조/동시성" 관점에서 다시 발산

지금까지는 전부 "저장 버튼 하나를 누르는 시점"의 diff·문구를 검증했다. 이번엔 각도를 완전히 바꿔서 (1) 알림 시스템과 붙어있지 않은 다른 기능(합치기)이 알림 대상 이벤트를 우회하는지, (2) 여러 사람이 동시에 편집하면 diff 계산 자체의 전제(내가 들고 있는 "이전 상태"가 진짜 최신인가)가 깨지는지를 봤다.

### M-1. ⚠️ **확인된 문제(중요)**: 프로그램 합치기(merge)가 사실상 "삭제"인데 삭제 알림을 안 탐
`doProgramMerge()`([js/admin.js:2049](js/admin.js#L2049))를 다시 보면, 원본(src) 프로그램의 회차를 전부 대상(tgt)으로 옮기거나(`update program_id`) 중복이면 지운다(`delete`). 둘 중 어느 쪽이든 결과적으로 **src 프로그램의 `education_sessions` 행이 0개**가 되고, `buildPrograms()`는 세션이 하나도 없는 program_id는 아예 그룹을 안 만드니 src는 앱 화면에서 조용히 사라진다. 그런데 이 함수는 `notifyDeletedSlack`은커녕 어떤 notify 함수도 호출하지 않는다.

**왜 지금 더 중요해졌나**: 예전(2차 검증) 때는 "합치기 알림이 없다"는 걸 "그냥 없는 기능"으로 봤는데, 지금은 정확히 이 상황(프로그램이 사실상 없어짐)을 위한 🗑️ 삭제 알림이 이미 있다. 즉 같은 결과(프로그램이 사라짐)인데 경로에 따라 한쪽(직접 삭제)은 알림이 가고 다른 쪽(합치기)은 안 가는 **불일치**가 생김. 누군가 그 src 프로그램의 예전 Slack 링크를 눌러도 이제 아무것도 안 뜨는 것도 완전히 삭제 케이스와 동일한 문제.

**고치는 방법**: `doProgramMerge()`에서 DB 반영 직전에 `notifyDeletedSlack(src, srcP, numberSessionsForSlack(srcP.sessions), confirmedAssigneeEmails(src).map(nameOf))`를 호출하면 됨 — 이미 있는 삭제 알림 인프라를 그대로 재사용 가능(새로 만들 것 없음).

### M-2. ⚠️ **확인된 문제(구조적, 우선순위는 상황 따라 다름)**: 동시 편집 시 오래된 스냅샷 기준으로 diff해서, 이미 다른 사람이 지운 회차가 조용히 부활하고 Slack도 "무변경"이라고 잘못 말함
시나리오: 프로그램이 원래 3회차(8/4, 8/11, 8/18). 운영자 B가 8/11 회차를 지우고 저장(DB엔 이제 2회차만 있음). 그런데 운영자 A는 B가 저장하기 전 화면(3회차짜리, 새로고침 안 한 오래된 상태)을 이미 열어둔 채 있다가, 아무것도 안 건드리고 그냥 "저장"을 누름.

```
A가 저장할 때 계산되는 changes: []
(알림 없음 — A 입장에선 아무것도 안 바꿨으니까)

실제로 벌어진 일: B가 지운 8/11 회차가 A의 저장으로 인해 DB에 새 id로 다시 살아남(부활). Slack엔 아무 신호도 없음.
```
**원인**: `computeScheduleChanges`는 "A가 들고 있는 이전 상태"(`state.programById[id].sessions`, A의 화면이 로드된 시점의 스냅샷)와 "A의 폼"만 비교한다 — **DB의 실제 최신 상태**와 비교하는 게 아니다. A 입장에서 자기가 본 것과 자기가 제출한 게 똑같으니 "무변경"이라 판단하지만, 그 사이 B가 만들어놓은 변화(8/11 삭제)를 A의 저장이 통째로 덮어써버린다.

**심각도 판단**: 이건 지금까지 찾은 것들과 성격이 다르다 — "diff 로직 자체의 버그"가 아니라 **애초에 동시 편집에 대한 안전장치(낙관적 동시성 제어, 예: updated_at 비교나 버전 체크)가 없다**는 더 근본적인 설계 이슈의 한 증상이다. 실제로 이 프로그램을 운영팀 여러 명이 동시에 잘 안 건드린다면 발생 빈도는 낮겠지만, 한번 발생하면 "삭제한 회차가 되살아났는데 아무도 모른다"는 눈에 안 띄는 데이터 정합성 문제로 이어짐. 고치려면 diff 로직 수정 정도가 아니라 저장 시점에 "내가 마지막으로 읽은 이후 이 프로그램이 바뀌었는지" 확인하는 절차 자체를 추가해야 해서, 이번 검증 스코프(알림 문구 정확성)보다 큰 작업임 — 일단 발견만 보고.

### M-3. (참고, 우선순위 낮음) 이메일 대소문자가 다르면 동일인을 다른 사람으로 오인해 헛된 +/- 알림
```
🔄 담당 멘토가 변경됐습니다 — *[테스트 회사]* 테스트 제목
+ 김사원 추가
- 김사원 제외
...
담당 멘토(현재): 김사원
```
`asd@asd.com`(이전)과 `ASD@ASD.COM`(이후)이 같은 사람인데 대소문자만 다르면, 이메일 기준 diff(H-2 수정)가 이걸 "다른 사람"으로 오인해서 실제로는 아무 변화가 없는데도 "+ 추가 / - 제외"가 동시에 나가버림. 다만 실제 앱에서 멘토를 고를 땐 항상 명단 드롭다운에서 선택하지 이메일을 직접 타이핑하지 않으므로, 같은 사람 이메일의 대소문자가 실제로 서로 다르게 저장될 경로가 마땅치 않아 실전에서 터질 가능성은 낮다.

---

## 최종 요약(갱신)

| 항목 | 상태 |
|---|---|
| M-1: 합치기가 삭제 알림을 우회 | **수정 완료** — `doProgramMerge()`에서 DB 반영 직전에 src 스냅샷을 떠 두고, 성공 후 `notifyDeletedSlack(src, srcP, srcSessionsSnapshot, srcMentorNames)` 호출 추가. 새 코드/타입 추가 없이 기존 삭제 알림 인프라 재사용. |
| M-2: 동시 편집 시 조용한 데이터 부활 | **수정 완료** — 저장 직전 낙관적 동시성 체크 추가(아래 참고) |
| M-3: 이메일 대소문자 오탐 | 참고만, 실전 발생 경로 없음(드롭다운 선택이라) |

### M-2 수정 내용

전체 낙관적 동시성 제어(버전 컬럼 추가 등) 대신, 스키마 변경 없이 저장 직전에 "내가 화면을 띄운 뒤로 이 프로그램이 실제로 바뀌었는지"만 가볍게 재확인하는 방식으로 구현했다.

- `checkStaleBeforeSave(id)`([js/admin.js](js/admin.js)) 신설: 저장 직전 `education_sessions`/`assignments`를 다시 조회해서, 이미 검증된 `computeScheduleChanges`(회차)와 새 헬퍼 `assignmentsSnapshotKey`(배정)로 "내가 들고 있던 스냅샷"과 "지금 DB의 실제 상태"를 비교한다.
- `saveProgram()`은 편집 모드(`id` 있음)일 때 이 체크를 제일 먼저 하고, 다르면 DB에 아무것도 안 쓰고 "이 교육이 방금 다른 곳에서 수정됐어요. 화면을 새로고침한 뒤 다시 시도해주세요." 토스트만 띄운 뒤 종료한다.
- 체크 자체가 네트워크 오류 등으로 실패하면 평소처럼 저장을 진행한다(과잉 차단 방지 — 이 체크는 부가적인 안전장치이지 저장을 막는 게 목적이 아님).
- 완벽한 락은 아니다(체크와 실제 저장 사이의 아주 짧은 틈은 여전히 이론상 남음) — 하지만 실제로 문제가 되는 경우(화면을 오래 띄워두거나 다른 탭에서 먼저 저장한 뒤 예전 화면으로 돌아와 저장)는 이걸로 대부분 잡아낸다.
- `done`/`note`(완료 여부·특이사항) 필드는 비교 대상에서 제외했다 — 이건 Slack 알림에 영향을 주지 않는 낮은 위험도 필드라 스코프 밖으로 뒀다.

재검증(M-2 원래 시나리오 그대로 재현):
```
sessionsStale (true여야 정상 차단): true
assignmentsStale (변화 없을 때 false, 다른 사람이 배정 추가하면 true): 둘 다 기대대로 나옴
```
정상 재저장(아무도 안 건드린 경우)엔 이 체크가 `false`를 반환해 기존 흐름 그대로 진행됨을 확인.

---

## N. 6차 검증 — 방금 만든 동시성 체크 자체를 스트레스 테스트 + 같은 부류의 새 코드 경로

M-2 체크는 막 만든 코드라 아직 전혀 검증 안 했다. 그리고 그 체크를 만들면서 "쓰기가 실제로 뭔가를 바꿨는지 확인 안 하고 성공으로 간주"하는 같은 패턴이 다른 곳(`unassign`)에도 있는지 다시 훑었다.

### N-1. `checkStaleBeforeSave` 자체 검증
- **배정 목록 순서만 다르고 내용은 같음 → 오탐 없어야 함**:
  ```
  N-1a 순서만 다름(오탐 없어야 함, false 기대): false
  ```
  확인: 정상, 배열 순서와 무관하게 정확히 판단.
- **새 프로그램 생성 시(`id` 없음)**: 코드상 `if (id && (await checkStaleBeforeSave(id)))`라 새로 만들 땐 이 체크 자체가 실행 안 됨 — 맞는 설계(새 프로그램은 "이전 상태"가 없으니 비교 대상이 없음).
- **체크 자체가 네트워크 오류로 실패**: `freshSessions.error || freshAssignments.error`면 `null` 반환 → 바깥의 `if (id && (await checkStaleBeforeSave(id)))`가 거짓이 되어 저장이 평소처럼 진행됨(fail-open, 의도한 대로).

### N-2. ⚠️ **확인된 문제(새로 발견)**: `unassign()`이 실제로 지워졌는지 확인 안 하고 알림을 보내서, 동시에 같은 배정을 두 번 해제 시도하면 "제외" 알림이 중복으로 나감
`saveProgram()`의 배정 insert/delete는 이미 `assignErr`로 "진짜 반영됐는지" 확인하도록 고쳐놨는데([js/admin.js](js/admin.js) 참고), **`unassign()`은 이 검사가 없다**:
```js
const { error } = await window.sb.from("assignments").delete().eq("id", id);
if (error) return Util.toast("해제 실패: " + error.message);
```
문제는 PostgREST/Supabase에서 `DELETE ... WHERE id = X`가 **매칭되는 행이 0개여도 에러가 아니라 그냥 성공**으로 온다는 점. 시나리오: 운영자 A와 B가 같은 배정(김사원, id="x1")을 화면에 띄워두고 거의 동시에 "해제" 버튼을 누르면:
1. A의 delete가 먼저 실제로 지움 → 성공, `notifyMentorSlack`으로 "- 김사원 제외" 알림 발송(정당한 알림).
2. B의 delete는 이미 없는 행이라 **아무것도 안 지웠는데도** `error`가 없어서 그대로 통과 → B도 자기가 들고 있던(새로고침 안 한) `wasConfirmed=true` 기준으로 판단해서 **똑같은 "- 김사원 제외" 알림을 한 번 더 발송**.

`assignPerson()`(배정 추가)은 반대로 이 문제가 없다 — `(program_id, person_email)` unique 제약 덕분에 두 번째 insert가 진짜 DB 에러로 막혀서 `if (error) return...`에 정확히 걸림. 삭제 쪽만 "0건 지움 = 성공"이라 이 보호가 없는 비대칭 구조.

**고치는 법**: `.delete().eq("id", id).select()`로 바꿔서 실제로 지워진 행을 돌려받고, 빈 배열이면("이미 다른 곳에서 지워짐") 토스트만 띄우고 알림 없이 종료 — `saveProgram()`의 `assignErr` 검사와 같은 원칙.

### N-3. (참고) 동시성 체크는 세션·배정만 보고, 프로그램 메타데이터(회사명·상태 등)는 대상이 아님
M-2에서 만든 `checkStaleBeforeSave`는 회차·배정만 재확인한다. 만약 A가 편집 모달을 열어둔 사이 B가 그 프로그램의 **상태**만 "완료"로 바꿔 저장했다면, A가 그 위에 저장해도(회차·배정은 안 건드렸으니) 이 체크는 안 걸리고, A의 폼에 남아있던 예전 상태값("모집중")으로 B의 변경이 조용히 덮어써진다. I-1~I-3에서 이미 "메타데이터 단독 변경은 스코프 밖"으로 정리했던 것과 같은 경계선 — 새로 만든 동시성 체크에도 그대로 이어지는 한계라 참고로 남긴다. 지금 스코프(Slack 알림 정확성) 밖이라 손 안 댐.

### N-4. (참고) 일정 알림과 멘토 알림이 `await` 없이 동시에 발사돼서, Slack 도착 순서가 뒤바뀔 수 있음
`saveProgram()` 끝부분:
```js
notifyScheduleSlack(pid, base, scheduleEvent, ...);
if (id && !assignErr && (toAdd.length || toRemove.length)) notifyMentorSlack(pid, base, ...);
```
둘 다 `await` 없이 호출돼서(fire-and-forget) 두 개의 네트워크 요청이 거의 동시에 나간다. 응답 속도에 따라 Slack 채널엔 🔄가 ✏️보다 먼저 뜰 수도 있음. 다만 각 메시지가 이미 "전체 일정"을 포함한 완결된 정보라(D-1에서 검증한 설계 원칙) 순서가 바뀌어도 이해에 지장은 없어 보임 — 버그라기보단 관찰 사항.

---

## 최종 요약(갱신)

| 항목 | 상태 |
|---|---|
| N-1: 동시성 체크 자체 회귀 없음 | 확인 완료 |
| N-2: unassign 중복 알림(동시 해제) | **수정 완료** |
| N-3: 동시성 체크가 메타데이터는 커버 안 함 | **수정 완료** |
| N-4: 알림 두 개 발사 순서 비보장 | **수정 완료** |

### N-2 수정 내용
`unassign()`의 delete 호출에 `.select()`를 붙여 실제로 지워진 행을 돌려받는다. `data`가 비어있으면(이미 다른 곳에서 지워짐) "이미 다른 곳에서 해제된 배정이에요" 토스트만 띄우고 알림 없이 종료 — `saveProgram()`의 `assignErr` 검사와 같은 원칙("쓰기가 실제로 뭔가를 바꿨는지 확인 후에만 알림").

### N-3 수정 내용
`checkStaleBeforeSave`가 이제 `education_sessions`에서 회사명·제목·상태·필요 멘토/퍼실 인원·메모까지 같이 조회해서, 회차·배정 diff에 안 걸리더라도 이 필드들이 그 사이 바뀌었으면 저장을 막는다. 장소(location)는 따로 안 봄 — 기본 장소가 바뀌면 그걸 물려받는 회차들의 location 자체가 회차 diff에서 이미 잡히므로 중복 비교를 피함.

재검증:
```
변화 없음(false 기대): false
상태만 변경(true 기대): true
필요인원만 변경(true 기대): true
memo null vs 빈문자열(false 기대): false
```

### N-4 수정 내용
`saveProgram()` 끝의 `notifyScheduleSlack`/`notifyMentorSlack` 호출에 `await`를 붙여 순서대로 보내도록 함. 모달은 이미 닫히고 `loadAll()`도 이미 시작된 뒤라 화면 반응성엔 영향 없음.

전 시나리오(27개) 재실행 결과 회귀 없음 확인. 배포 완료.

---

## 7차 검증 — 최종 점검

A~N까지의 실제 코드(`saveProgram`/`unassign`/`assignPerson`/`deleteProgram`/`doProgramMerge`/`checkStaleBeforeSave`/`notify-schedule-slack/index.ts` 전문)를 처음부터 다시 읽으며 이 문서에 적힌 내용과 한 줄씩 대조했다. 모든 절(A~N)의 코드 스니펫·문구 예시·수정 내용이 지금 배포된 코드와 정확히 일치함을 확인 — 문서와 코드 사이의 드리프트는 없었다.

이번 라운드에서 진짜 새로운 버그 범주는 못 찾았다(N까지 이미 메시지 정확성 → 이름/이메일 처리 → 마크다운 → 메타데이터 스코프 → 삭제/합치기 우회 → 동시성까지 훑어서, 더 짜내려 하면 억지스러운 이론적 케이스만 남는 지점에 도달했다고 판단). 대신 실제로 발견된 문제 하나는 **코드가 아니라 이 문서 자체**였다: 라운드마다 "종합/최종 요약"을 새로 추가하면서 예전 요약(예: L 직후 "남은 것: H-3 하나뿐")이 그 뒤에 나온 M-1/M-2/N-2/N-3/N-4 수정 내용을 반영 못 한 채 stale로 남아있었음 — 파일 맨 위에 안내문 추가하고, 아래에 전체를 대체하는 최종 결론을 새로 씀.

## 전체 최종 결론

**고친 것 (8건)**
| ID | 내용 | 커밋 |
|---|---|---|
| G-1 / H-1 | 회사명·교육명·멘토 이름의 Slack 마크다운 특수문자(`*_~\``) 이스케이프 | `escSlack()` 추가 |
| H-2 | 멘토 diff를 표시 이름이 아닌 이메일 기준으로 — 동명이인 교체 시 알림이 사라지던 것 수정 | beforeEmails/afterEmails 전환 |
| I-1~I-3 (방향 전환) | 세션 불변인데 상태만 "취소"로 바뀌는 경우 무알림 → 실사용 데이터 확인 후 "삭제 알림" 신설로 대응 | 5번째 알림 타입 `deleted` |
| M-1 | 프로그램 합치기가 원본을 사실상 삭제하는데 알림 우회 | `doProgramMerge()`에 `notifyDeletedSlack` 연결 |
| M-2 | 동시 편집 시 낡은 스냅샷으로 저장하면 남의 변경을 조용히 덮어씀 | `checkStaleBeforeSave()` 신설 |
| N-2 | `unassign()` 동시 클릭 시 중복 "제외" 알림 | delete에 `.select()` 붙여 실제 반영 여부 확인 |
| N-3 | 동시성 체크가 메타데이터(상태 등)는 못 봄 | `checkStaleBeforeSave`에 메타데이터 비교 추가 |
| N-4 | 알림 두 개 발사 순서 비보장 | `await`로 순차 발송 |

**의도적으로 안 고친 것 (스코프 밖 판단, 재발굴 불필요)**
| ID | 내용 | 이유 |
|---|---|---|
| G-2 | company/title 둘 다 빈 문자열이면 헤더가 지저분함 | 프론트 검증으로 앱에서는 도달 불가능 |
| H-3 | 이름이 완전히 같은 두 멘토를 동시 배정하면 "담당 멘토(현재)" 줄이 이름 기준 dedup이라 표시가 줄어듦 | diff 자체는 정확함(H-2로 고침), 표시만 드문 조합에서 어색함 — 우선순위 낮음 |
| I-3 잔여 | needed_mentors/needed_facilitators/client/title/memo 단독 변경은 여전히 무알림 | 팀 전체에 실시간으로 알릴 만큼 급하지 않다고 판단(의도적 스코프) |
| M-3 | 이메일 대소문자 다르면 동일인 오탐 | 멘토는 항상 드롭다운 선택이라 실제 발생 경로 없음 |
| N-3 잔여 | (수정했지만) 장소(location) 단독 필드는 메타데이터 체크 대상에서 의도적으로 제외 | 회차 diff에서 이미 잡혀서 중복 비교 방지 |

이 표 두 개가 지금 시점의 정확한 전체 상태다. 앞으로 이 기능을 더 건드릴 일이 있으면, 위쪽 라운드별 기록(A~N)은 "왜 이렇게 짰는지"의 근거로 참고하고, 현재 뭐가 됐고 안 됐는지는 이 표만 보면 된다.

---

## O. 사용성(UX) 관점 검토 — "코드가 맞나"가 아니라 "받는 사람이 쓸 만한가"

지금까지(A~N)는 전부 "메시지가 정확한가"였다. 이번엔 관점을 바꿔서, 팀원이 실제로 Slack에서 이 알림을 받았을 때의 체감을 봤다. 검토 결과 5가지가 나왔고, 그 중 2개를 고쳤다.

- **유지(문제 없음)**: ① 누가 바꿨는지 표기 없음, ④ "미정"이 자주 뜸, ⑤ 단일 회차일 때 변경사항≈전체일정 중복 — 실사용상 감수 가능이라 그대로 둠.

### O-1. (수정) 합치기가 "🗑️ 삭제"로 떠서 오해를 부름
합치기는 원본을 대상 교육으로 흡수하는 건데, 예전엔 `notifyDeletedSlack`을 그대로 써서 "🗑️ [넥센타이어] 교육이 삭제됐습니다"로 나갔다. 받는 사람은 교육이 통째로 없어진 줄 알고 놀랄 수 있다. → 6번째 알림 타입 `merged` 신설:
```
🔀 다른 교육에 합쳐졌습니다 — *[넥센타이어]* AX비즈 파일럿
→ *[넥센타이어 2기]*(으)로 합쳐졌어요
전체 일정(합치기 전 마지막 상태):
• 1회차 8/4(화) 14:00~18:00 · 마곡
• 2회차 8/11(화) 14:00~18:00 · 마곡
담당 멘토: 김사원
🔗 앱에서 확인: .../?program=<대상 교육 id>
```
"없어진 게 아니라 어디로 갔는지"가 문구·링크(대상 교육을 가리킴)로 명확해짐. `doProgramMerge()` → `notifyMergedSlack()` 호출로 교체.

### O-2. (수정) 일정+멘토를 한 번에 바꾸면 알림이 2개 연달아 옴
편집 저장 한 번에 일정도 바뀌고 멘토도 바뀌면 ✏️와 🔄가 연속으로 떠서 "중복인가?" 싶었다. → `updated` 메시지가 멘토 변경(added/removed)을 함께 실어 **한 메시지**로 합침:
```
✏️ 일정이 수정됐습니다 — *[테스트 회사]* 테스트 제목
변경사항:
• 8/4(화) 10:00~12:00 · 테스트 장소 → 8/4(화) 11:00~13:00 · 테스트 장소

담당 멘토 변경:
+ 운영팀 추가
- 김사원 제외

전체 일정:
• 1회차 8/4(화) 11:00~13:00 · 테스트 장소
담당 멘토: 운영팀
🔗 앱에서 확인: ...
```
`saveProgram()`에서 "일정 변경 && 멘토 변경 && updated"면 `notifyScheduleSlack`에 mentorDiff를 실어 1개로, 그 외(일정만/멘토만)는 기존처럼 각각 1개로 보낸다. 재검증: 일정만 바뀐 경우 "담당 멘토 변경" 섹션이 안 뜨는 것, 멘토만 바뀐 경우 기존 🔄가 그대로 나가는 것 모두 확인.

전 시나리오(27개) 재실행 + O-1/O-2 신규 시뮬레이션까지 회귀 없음 확인. 배포 완료. (알림 타입은 이제 6개: created/updated/assigned/changed/deleted/merged.)
